\* As EmojiProvider_gil.cfg but with a reader-preferring RwLock.
\* Expected: FAIL as well: the deadlock does not depend on the lock policy.
SPECIFICATION Spec
CONSTANTS
    defaultInitValue = defaultInitValue
    Threads = {1, 2}
    Setters = {1}
    ReaderCalls = 2
    ReaderText = {"plain", "emoji"}
    DelBehaviours = {"release_gil"}
    LookupReenters = FALSE
    WriterPref = FALSE
    Fixed = FALSE
    MaxDepth = 1
CHECK_DEADLOCK TRUE
