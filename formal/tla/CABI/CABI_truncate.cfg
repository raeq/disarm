SPECIFICATION Spec
\* The code as written; the client shortens a heap string it owns, then frees it.
\* (NoWriteToStatic is not checked here, so the search reaches the heap case.)
CONSTANTS
    MaxCalls = 2
    Inputs = {"valid"}
    ClientWrites = TRUE
    Fixed = FALSE
INVARIANTS TypeOK NoLayoutMismatch
