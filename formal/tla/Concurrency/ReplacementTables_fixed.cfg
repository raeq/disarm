\* Proposed fix: one Arc<automaton> snapshot per batch, taken before py.detach.
\* Poisoned too, to show the attached snapshot also removes the GIL wait.
\* Expected: PASS.
SPECIFICATION Spec
CONSTANTS
    Writers = {1, 2}
    Readers = {3}
    OpsPerWriter = 2
    Items = 3
    ChunkSize = 2
    WriterPref = TRUE
    Poisoned = TRUE
    Snapshot = TRUE
INVARIANTS TypeOK ACInSync BatchSingleVersion BatchLinearizable
CHECK_DEADLOCK TRUE
