SPECIFICATION Spec
\* The code as written; the client stores into a string it owns (disarm.h returns char *).
CONSTANTS
    MaxCalls = 2
    Inputs = {"valid"}
    ClientWrites = TRUE
    Fixed = FALSE
INVARIANTS TypeOK NoWriteToStatic
