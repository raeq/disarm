\* Real code. Does one transliterate(list) call see one table version?
\* Expected: FAIL (BatchSingleVersion). Repro r4.
SPECIFICATION Spec
CONSTANTS
    Writers = {1}
    Readers = {2}
    OpsPerWriter = 1
    Items = 2
    ChunkSize = 2
    WriterPref = TRUE
    Poisoned = FALSE
    Snapshot = FALSE
INVARIANTS TypeOK BatchSingleVersion
CHECK_DEADLOCK TRUE
