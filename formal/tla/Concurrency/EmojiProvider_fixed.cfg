\* Proposed fix (old handle dropped after the guard is released), every __del__
\* behaviour, re-entrant lookup, 3 threads. Expected: PASS.
SPECIFICATION Spec
CONSTANTS
    defaultInitValue = defaultInitValue
    Threads = {1, 2, 3}
    Setters = {1}
    ReaderCalls = 2
    ReaderText = {"plain", "emoji"}
    DelBehaviours = {"noop", "release_gil", "reenter_set", "reenter_demojize"}
    LookupReenters = TRUE
    WriterPref = TRUE
    Fixed = TRUE
    MaxDepth = 1
INVARIANTS TypeOK NoGuardAcrossPython NoSelfWait
CHECK_DEADLOCK TRUE
