\* Real code, benign __del__, re-entrant provider.lookup (demojize and
\* set_emoji_provider from inside lookup), one extra thread swapping providers.
\* Expected: PASS. Repro r0 (negative control).
SPECIFICATION Spec
CONSTANTS
    defaultInitValue = defaultInitValue
    Threads = {1, 2}
    Setters = {2}
    ReaderCalls = 2
    ReaderText = {"plain", "emoji"}
    DelBehaviours = {"noop"}
    LookupReenters = TRUE
    WriterPref = TRUE
    Fixed = FALSE
    MaxDepth = 1
INVARIANTS TypeOK NoGuardAcrossPython NoSelfWait
CHECK_DEADLOCK TRUE
