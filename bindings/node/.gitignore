# Rust build output (Cargo.lock is gitignored repo-wide, as for the core and the gem)
target/

# npm
node_modules/

# Build artifacts (generated by `napi build` + `tsc`; shipped as npm prebuilds, never committed)
*.node
binding.js
binding.d.ts
index.js
index.d.ts
