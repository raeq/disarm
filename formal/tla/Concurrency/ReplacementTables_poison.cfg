\* Real code, with GLOBAL_REPLACEMENTS_AC poisoned (recover_lock attaches to the
\* GIL while holding the recovered read guard, inside py.detach).
\* Expected: FAIL (deadlock). Latent: no reachable panic poisons this lock today.
SPECIFICATION Spec
CONSTANTS
    Writers = {1}
    Readers = {2}
    OpsPerWriter = 2
    Items = 1
    ChunkSize = 1
    WriterPref = TRUE
    Poisoned = TRUE
    Snapshot = FALSE
INVARIANTS TypeOK
CHECK_DEADLOCK TRUE
