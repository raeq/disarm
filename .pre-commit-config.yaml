repos:
  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        description: Format Rust source files with cargo fmt
        language: system
        entry: cargo fmt --
        types_or: [rust]
        pass_filenames: false

      - id: cargo-clippy
        name: cargo clippy
        description: Lint Rust source files with clippy (mirrors CI -D warnings)
        language: system
        entry: cargo clippy --no-default-features -- -D warnings
        types_or: [rust]
        pass_filenames: false

      - id: ruff-check
        name: ruff check
        description: Lint Python source files with ruff
        language: system
        entry: ruff check --fix
        types: [python]

      - id: ruff-format
        name: ruff format
        description: Format Python source files with ruff
        language: system
        entry: ruff format
        types: [python]
